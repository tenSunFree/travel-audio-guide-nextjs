# Copy this file to .env.local and fill in the actual values.
# .env.local is excluded by .gitignore and should not be committed.

# Phase 2 (required only after connecting to the Go API)
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8080